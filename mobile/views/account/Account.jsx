import { StyleSheet, Text, View } from "react-native";
import React from "react";
import ScreenHeader from "../components/ScreenHeader";
const Account = () => {
  return (
    <View>
      <ScreenHeader screen={"Account"} />
    </View>
  );
};

export default Account;

const styles = StyleSheet.create({});
